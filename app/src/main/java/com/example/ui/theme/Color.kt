package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// --- Vibrant Palette Theme (Dynamic Google Blue Architecture) ---

// Brand Colors aligned to Vibrant Palette
val TealPrimary = Color(0xFF1B6EF3)       // Vibrant Royal Blue (Primary Action Base)
val TealSecondary = Color(0xFF0B57D0)     // Deep Digital Blue (Accent Highlights)
val TealAccent = Color(0xFF4C8DF6)        // Sky Blue Accent (Radar / Live Indicator)
val AmberWarning = Color(0xFFF59E0B)      // Warning amber (94% attendee highlights / Offline markers)

// Light Mode Tokens
val LightBackground = Color(0xFFF7F9FB)   // Soft off-white canvas
val LightSurface = Color(0xFFFFFFFF)      // Floating cards container base
val LightTextPrimary = Color(0xFF0F172A)  // Slate 900 primary labels
val LightTextSecondary = Color(0xFF475569) // Slate 600 details
val LightBorder = Color(0xFFC2D7F7)       // Elegant blue border/divider outline

// Dark Mode Tokens
val DarkBackground = Color(0xFF040D1A)    // High contrast midnight navy backdrop
val DarkSurface = Color(0xFF0B1E36)       // Rich deep slate navy card surface
val DarkTextPrimary = Color(0xFFF7F9FB)   // Brilliant off-white labels
val DarkTextSecondary = Color(0xFF90B5E8)  // Medium slate detail labels
val DarkBorder = Color(0xFF1E3A5E)        // Deep accent neon outline border

